spring:
  application:
    name: netflix-user
  profiles:
    active:
      dev
  cloud:
    config:
      uri: http://netflix-configuration-server.sa-east-1.elasticbeanstalk.com

server:
  port: 8090